db: "sink.jsonl"

reviewer: mailto:user@example.com

labels:
  annotate:
    question: Annotate
    type: annotate

flows:
  simple:
    steps:
      - run_embedded: "generator-file gendocs.jsonl"

      - run_embedded: "remove-reviewed"
